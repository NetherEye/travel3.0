let url = "/proxy";
//*********************上线时候打开注释打包发给后台*******************************

let oldData = {
	'oldData': url + '/api/oldData', // 我填报的 get 获取 put 修改
	'export': url + '/api/export/oldData', // 导出2.0旧数据
}


export default oldData;

